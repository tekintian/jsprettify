{
  "name": "jsprettify",
  "version": "1.0.0-ncc",
  "description": "Prettify and decompress your JavaScript code with jsprettify",
  "main": "dist/jsprettify",
  "bin": {
    "jsprettify": "./dist/jsprettify"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "prettify": "node src/index.js",
    "clean": "rm -rf dist",
    "build": "npm run clean && npx @vercel/ncc build src/index.js -o dist --minify && chmod +x dist/index.js && mv dist/index.js dist/jsprettify"
  },
  "dependencies": {
    "prettier": "^3.8.0",
    "semver": "^7.3.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  },
  "keywords": [
    "javascript",
    "formatter",
    "prettify",
    "beautifier"
  ],
  "author": "tekintian@gmail.com (https://dev.tekin.cn)",
  "gitRepository": "https://github.com/tekintian/jsprettify",
  "license": "ISC",
  "files": [
    "dist/",
    "run-unix.sh",
    "run-windows.ps1"
  ]
}
